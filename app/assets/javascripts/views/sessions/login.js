NG.Views.Login = Backbone.View.extend({
	template: JST["sessions/login"],
	render: function() {
		// var that = this;

		// var renderedLoginPage = that.template();
		// that.$el.css({"position": "inherit"});
		// that.$el.html(renderedLoginPage);
		// // that.$el.find("#login-div").toggleClass("hidden");

		// return that;
	},
})